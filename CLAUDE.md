# 投研系统 — 项目协作指令

> 本文件给 Claude Code 读取，提供项目背景与协作约定。Claude 启动于本目录时会自动加载，跨设备一致。

## 项目概述

- **项目**：基金公司投研系统 IT 产品设计
- **负责人角色**：基金行业 IT 产品经理
- **目标**：设计一套支撑投研业务的 IT 系统，沉淀 PRD、方案设计、SQL、原型等产出物
- **协作方式**：本人可能从多台设备接入，所有进度须可被任意设备接续（详见 README.md）

## 业务领域

- **行业**：公募基金 / 资管
- **涉及业务线**：投研（研究、投资决策、组合管理、风控等）
- **常用术语约定**：在下方补充，统一用词，避免 AI 混用口径
  - （待补充：如 基金产品 / 投资组合 / 持仓 / 净值 / 归因 等的精确定义）

## 协作约定

### 文档与产出
- 产出物放 `docs/`（prd / design / sql）和 `prototypes/`
- 文档优先 Markdown；表格、流程用文本可 diff 的方式表达
- SQL 脚本放 `docs/sql/`，文件名含用途与日期，如 `holdings_snapshot_20260811.sql`
- 大文件 / 二进制（Axure .rp、图片）放 `prototypes/`，或仅记录链接

### 进度维护（跨设备接续的关键）
- **每次有实质性推进，更新 `README.md` 的「📍当前进度」**：阶段、最近更新日期、下一步、勾选历史进度项
- 重大决策写入 `docs/design/`，并在 `.claude/memory/` 留一条记忆（user/feedback/project 类）

### 多设备
- 进度靠文件承载，不靠对话记录；假设对话历史在设备间不可见
- 换设备接手时，以 `README.md` + 本文件为唯一事实来源

### 安全与合规
- 投研内容可能含业务敏感信息
- **不要把敏感数据（真实持仓、客户信息、未公开财务数据）写入仓库**，即使会推到私有库
- 不确定能否上云的内容，先问我

## UI 设计规范

投研系统前端一律遵循 Value Partners 设计系统：完整规范见 [`docs/design/ui-spec.md`](docs/design/ui-spec.md)。做任何投研 UI（页面、组件、原型代码）前必须遵守。以下是**不可违反的铁律**（仍需查原文取完整 token/组件细则）：

- **涨跌色为港式惯例**：上涨 = 红 `#B23A48`，下跌 = 绿 `#20A06E`。绝不套用"绿涨红跌"。
- **主色是 `--vp-green: #20A06E`**，用于顶栏/主操作/链接/关键数值。`--vp-lime: #BDD646` 仅限 Logo 内部，绝不当界面主色。
- **标题克制**：最大常规标题 24px，关键 KPI 数字才到 32px，不要造"发布会"大标题。
- **字体**：标题用 Poppins，正文用 Open Sans；中文靠 `PingFang TC`/`Microsoft JhengHei` 后备。数字/时间/利率开 `font-variant-numeric: tabular-nums`。
- **整页背景 `--vp-mint-50: #F2F8F4`**（冷调浅薄荷绿），内容用纯白卡片承载。
- **复用 token，不另起近似色**：颜色/间距(4px基准)/圆角/阴影变量见规范第 3、6 节。
- **克制风格**：圆角轻、阴影很轻（带深绿色调），不用厚黑投影/玻璃拟态/高光渐变。

## 仓库结构（速览）

详见 `README.md`。要点：`docs/` 放设计产出，`.claude/memory/` 放项目级 AI 记忆，根目录两份 md 是进度主干。
