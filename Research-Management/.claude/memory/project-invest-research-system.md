# 占位说明

本目录存放**跟随项目、跨设备共享**的 AI 记忆。

与 Claude Code 默认的"自动记忆"（`~/.claude/projects/<路径>/memory/`，绑定本机绝对路径）不同，这里的记忆文件会被 Git 跟踪，任何设备 pull 下来都能读到。

当某条记忆需要跨设备共享时，把对应文件放到这里，并在项目根的 `CLAUDE.md` 或 `README.md` 里提及。
